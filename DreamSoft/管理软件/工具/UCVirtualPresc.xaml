<UserControl x:Class="DreamSoft.UCVirtualPresc"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Loaded="UserControl_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Width="100" Height="50" Foreground="Crimson" Click="btStart_Click" Name="btPresc_Start" HorizontalAlignment="Right" Margin="20">开 始</Button>
        <Button Grid.Column="1" Width="100" Height="50" Click="btStop_Click" Name="btPresc_Stop" HorizontalAlignment="Left" Margin="20">停 止</Button>
    </Grid>
</UserControl>
